import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-modal-change-detection-bug-demo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
